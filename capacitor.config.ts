import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Aplicativo Senac',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
